# DeepSeek API Configuration
# Get your API key from https://platform.deepseek.com/
# Leave empty to use fallback summary generation
DEEPSEEK_API_KEY=

# Nuclei CLI Configuration
# Path to the nuclei executable (default: "nuclei")
# Set this if nuclei is not in your PATH
NUCLEI_PATH=

# Scanner Mode
# Leave empty or set to "true" to try real Nuclei first (falls back to mock if unavailable)
# Set to "false" to force mock scanner mode
# Default: empty (tries real Nuclei first, falls back to mock)
USE_REAL_NUCLEI=
